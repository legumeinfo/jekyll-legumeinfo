// https://github.blog/changelog/2022-10-26-codespaces-customizable-initial-experience
{
	"onCreateCommand": "git submodule update --init && make install && make jbrowse-install",
	"postCreateCommand": "", // disable Oryx platform detection / old ruby installation
	"portsAttributes": {
		"4000": {
			"label": "jekyll",
			"onAutoForward": "openPreview"
		}
	},
}
